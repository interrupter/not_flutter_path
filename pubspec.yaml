name: not_flutter_path
description: Not not-path port for Flutter.
publish_to: 'none'
version: 0.1.0+1
environment:
  sdk: ">=2.17.1 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
